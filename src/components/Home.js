import React from "react";
import { Link } from "react-router-dom";
import "../App";

const Home = (props) => {
  return (
    <h1 style={{ textAlign: "center", color: "orange" }}>Welcome Home!</h1>
  );
};

export default Home;
